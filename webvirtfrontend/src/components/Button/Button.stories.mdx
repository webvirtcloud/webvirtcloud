import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { ArgsTable } from '@storybook/addon-docs';
import PlusIcon from '@heroicons/react/20/solid/PlusIcon';
import TrashIcon from '@heroicons/react/20/solid/TrashIcon';
import PencilSquareIcon from '@heroicons/react/20/solid/PencilSquareIcon';
import { Button } from './Button';

<Meta title="Design System / Components/Button" component={Button} />

```js
import { Button } from './Button';
````

## Usage 

<Canvas withSource="open">
  <Story name="base">
    <Button>
      My button
    </Button>
  </Story>
</Canvas>

<Canvas withSource="open">
  <Story name="variants">
    <div className="space-x-1">
      {['default', 'secondary', 'danger'].map((variant) => (
        <Button key={variant} variant={variant}>{variant}</Button>
      ))}
    </div>
  </Story>
</Canvas>

<Canvas withSource="open">
  <Story name="sizes">
    <div className="space-x-1">
      <Button size='sm'>Small</Button>
      <Button size='md'>Medium</Button>
      <Button size='lg'>Large</Button>
    </div>
  </Story>
</Canvas>

<Canvas withSource="open">
  <Story name="disabled">
    <Button size='md' disabled>Disabled</Button>
  </Story>
</Canvas>

<Canvas withSource="open">
  <Story name="with icons">
    <div className="flex items-center space-x-1">
      <Button variant='danger' startIcon={<TrashIcon width={14} height={14} />}>
        Delete
      </Button>
      <Button endIcon={<PlusIcon width={14} height={14} />}>
        Create
      </Button>
    </div>
  </Story>
</Canvas>

<Canvas withSource="open">
  <Story name="only icons">
    <div className="flex items-center space-x-1">
      <Button variant='danger' onlyIcon>
        <TrashIcon width={14} height={14} />
      </Button>
      <Button onlyIcon variant='secondary'>
        <PlusIcon width={14} height={14} />
      </Button>
      <Button onlyIcon>
        <PencilSquareIcon width={14} height={14} />
      </Button>
    </div>
  </Story>
</Canvas>

<Canvas withSource="open">
  <Story name="fullWidth">
    <Button size='md' fullWidth>Full width</Button>
  </Story>
</Canvas>

## Props 
<ArgsTable of={Button} />